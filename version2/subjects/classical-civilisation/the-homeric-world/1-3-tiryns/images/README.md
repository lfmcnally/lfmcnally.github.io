# Tiryns (1.3) — image manifest

The revision page references these per-lesson images as `images/<file>`.
Drop the files in here with these exact names. **✗** = still needed.

## At a glance (the paired figures)
- ✗ `tiryns-aerial.jpg` — aerial view of the citadel today, the low ridge in the Argolid plain
- ✗ `tiryns-plan.jpg` — groundplan showing the upper and lower citadel

## Around the citadel (floated detail photos, in page order)
- ✗ `cyclopean-walls.jpg` — the colossal Cyclopean walls
- ✗ `ramp-and-gate.jpg` — the great approach ramp up to the main gate
- ✗ `gallery.jpg` — a corbelled gallery (pointed vault) inside the walls
- ✗ `tholos-tomb.jpg` — the tholos tomb near the citadel

## Notes
- Landscape crops suit the two "at a glance" figures (shown at ~320px tall, side by side).
- The four "around the citadel" photos float at ~150px wide beside their notes, so
  reasonably square or portrait crops read best.
- Images are click-to-zoom (the shared lightbox is enabled on this page).
- Until a file is added, the page shows the image's alt text in its place — harmless,
  just swap each one in as you go.
